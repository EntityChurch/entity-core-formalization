\* NEGATIVE CONTROL #1 — the non-conformance §4.7 names in its own preamble: "an impl that
\* collapses several of these to one code ... is non-conformant". Every reject path emits
\* `connection_sequence_error`, so §4.7 row 6 (nonce) and row 9 (already established) become
\* indistinguishable to a client keying off result.data.code.
\* EXPECTED VERDICT: ReasonCodesDistinct VIOLATED.
\* Run: make tlc SPEC=ConnCodes CFG=ConnCodesCollapseBug
SPECIFICATION Spec
CONSTANT CollapseCodes = TRUE
CONSTANT WrongStatus = FALSE
CONSTANT PreHelloAuthRow = "r6_nonce"
INVARIANT ReasonCodesDistinct
