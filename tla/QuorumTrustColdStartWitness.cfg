\* §4.2 / §4.2.1 NON-VACUITY WITNESS. Asserted in order to be VIOLATED; the violation exhibits
\* the state and IS the pass. A clean run means the state is unreachable, and the results in
\* QuorumTrust.cfg would then be statements about a model that never gets there.
\*
\* Expected verdict: `Invariant WitnessColdStartExposed is violated.`
SPECIFICATION Spec

CONSTANTS
  NA                      = 3
  NQ                      = 2
  RawWrites               = TRUE
  AcceptInvalidates       = TRUE
  RawInvalidates          = FALSE
  ScopedInvalidation      = TRUE
  WalkClosedOverValidated = TRUE

INVARIANTS
  WitnessColdStartExposed
