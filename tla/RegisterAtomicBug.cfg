\* NEGATIVE CONTROL #1 — §6.2 five-write atomicity dropped (Atomic = FALSE): register publishes
\* the handler to the dispatch index with only manifest+iface written; grant/sig/types land
\* later. Expect a violation — a half-built handler is dispatch-visible (manifest without grant):
\* RegisterAllOrNothing / IndexMatchesTree / NoPartialResidue caught.
\* Run: make tlc SPEC=Register CFG=RegisterAtomicBug
SPECIFICATION Spec
CONSTANT Atomic = FALSE
CONSTANT GuardSystem = TRUE
CONSTANT WedgeReg = FALSE
INVARIANT NoPartialResidue
INVARIANT RegisterAllOrNothing
INVARIANT IndexMatchesTree
INVARIANT NoUserAtSystem
