\* T4 CLASSIFIER — CARRIED. The one component invariant the composed model really carries.
\*
\* READ THE VERDICT THE RIGHT WAY ROUND: here a VIOLATION is the GOOD outcome. It shows a
\* type-correct valuation that falsifies Conn's own DispatchedImpliesEstablished, which
\* proves the mapping does NOT force it — so Core's green on the same predicate
\* (CoreRefinesConn.cfg) is a fact about Core's §4.2 dispatch gate rather than about the
\* mapping. A clean green here would mean the opposite and would retire the claim.
\*
\* EXPECTED VERDICT: VIOLATION (state: cstate[1] = "sent" with conn[1] = "new").
\* Run: make tlc SPEC=CoreMapFree CFG=CoreMapFreeCarried
SPECIFICATION Spec
CONSTANT N = 2
CONSTANT RefPeer = 1
INVARIANT FreeConnDispatched
