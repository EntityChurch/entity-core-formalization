\* §4.2.1 NEGATIVE CONTROL for non-trigger 2 / TV-QF14. `RawInvalidates = TRUE` makes a raw
\* `tree:put` clear the cache, which §4.2.1 says it MUST NOT: "The cache reflects validated
\* quorum state, not raw tree state."
\*
\* Expected verdict: `Invariant NoInvalidationWithoutAcceptance is violated.`
SPECIFICATION Spec

CONSTANTS
  NA                      = 3
  NQ                      = 2
  RawWrites               = TRUE
  AcceptInvalidates       = TRUE
  RawInvalidates          = TRUE
  ScopedInvalidation      = TRUE
  WalkClosedOverValidated = TRUE

INVARIANTS
  NoInvalidationWithoutAcceptance
