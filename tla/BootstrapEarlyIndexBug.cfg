\* NEGATIVE CONTROL #1 — §6.9's informative sequence puts "6. Create capability grants"
\* before "7. Build dispatch index". This control builds the index from the step-4 manifests
\* instead, so a bootstrap handler is dispatchable before its grant exists — the §6.2
\* "manifest without grant" hazard on the one path that bypasses the register lifecycle.
\* EXPECTED VERDICT: BootAllOrNothing VIOLATED.
\* Run: make tlc SPEC=Bootstrap CFG=BootstrapEarlyIndexBug
SPECIFICATION Spec
CONSTANT AtomicPublish = FALSE
CONSTANT GateAll = TRUE
CONSTANT GateConnect = TRUE
INVARIANT BootAllOrNothing
