\* NEGATIVE CONTROL (F layer) — revocation gate dropped (GateRevocation = FALSE): a handler writes
\* the store even when the cap is revoked. Expect NoServeWhenRevoked VIOLATED (a peer served under
\* a revoked cap). Run: make tlc SPEC=Core CFG=CoreRevokeBug
SPECIFICATION Spec
CONSTANT Serialized = FALSE
CONSTANT GateEstablished = TRUE
CONSTANT GateRevocation = FALSE
INVARIANT StoreBounded
INVARIANT DispatchNeedsEstablished
INVARIANT ServeNeedsEstablished
INVARIANT NoServeWhenRevoked
PROPERTY EventuallyResolved
