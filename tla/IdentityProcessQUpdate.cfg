\* §6.3 / §3.6 FINDING -- NOT a negative control. Constants are the spec as written.
\*
\* THE ONE THAT COMPOSES WITH AN ALREADY-ROUTED FINDING. A `quorum-update` arriving over sync
\* at `system/quorum/{q}/event/{h}` -- the path §5.1 gives it, on the hook §10.2 declares --
\* fails §3.6 step 1 and is unbound by phase 2a. §3.3 says identity does not define the kind;
\* identity deletes it anyway.
\*
\* That is the ordinary way a membership change propagates. Composed with the already-routed Q1
\* (§QUORUM:4.2 falls through to the CREATION-TIME roster when the walk finds no head), the
\* roster cannot change because the updates do not survive to be walked -- a second, independent
\* route to the same end state, on a peer that has the identity extension installed.
\*
\* COHORT: Python branches quorum-update explicitly before phase 1 and validates it; Go's
\* negated `isIdentityKind` no-ops it; entity-core-rust branches ONLY quorum-publish, so on
\* Rust an arriving quorum-update falls through to `identity_verify_cert` and is unbound. One
\* of three ships this.
\*
\* THE STATE TLC EXHIBITS: `kind = 7`, `src = 2` (cross-peer sync).
\*
\* Expected verdict: `Invariant QuorumUpdateSurvivesArrival is violated.`
SPECIFICATION Spec

CONSTANTS
  PreRouteQPublish       = FALSE
  PreRouteQUpdate        = FALSE
  SeedOnPreRoute         = FALSE
  RevocationAdmitted     = FALSE
  Phase2aScoped          = TRUE
  EmitOnFailure          = TRUE
  EmitOnSuccess          = FALSE
  UnbindOnHandlerFailure = FALSE

INVARIANTS
  QuorumUpdateSurvivesArrival
