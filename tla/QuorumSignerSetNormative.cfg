\* §4.2 FINDING — NOT a negative control. Every constant is the green sweep's constant.
\*
\* §4.2 states a normative MUST and then gives an algorithm, and they are not the same function.
\* The MUST: "the most recent quorum-update whose not_before <= as_of (or the quorum entity's
\* initial signers if no such update exists), with no successor that was itself live at as_of."
\* The algorithm: `find_live_head(updates[0], ctx, as_of=as_of)`, fall through on null.
\*
\* This asserts they coincide. The counterexamples are the two rows above, plus the fact that
\* the sentence's "most recent" names no field and its "successor" does not say transitive --
\* both readings are declared in the module rather than assumed.
\*
\* Expected verdict: `Invariant SpecMatchesNormative is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = TRUE
  DescTemporal    = TRUE

INVARIANTS
  SpecMatchesNormative
