\* NEGATIVE CONTROL #4 — 0.8.2.6, "Address is evaluated before authentication". A peer that
\* consults authentication state first answers a foreign-namespace pre-establishment EXECUTE
\* with 401 `authentication_failed`. §4.7's own argument for why that is wrong: the 401
\* "directs the caller to authenticate and retry, and for a foreign-namespace address that
\* retry cannot succeed at any authentication state — so the 401 names a remedy that does
\* not exist".
\* EXPECTED VERDICT: AddressVerdictPhaseIndependent VIOLATED.
\* Run: make tlc SPEC=ConnCodes CFG=ConnCodesAddressOrderBug
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT SeqReadingBug = FALSE
CONSTANT AuthBeforeAddress = TRUE
CONSTANT RefusalMode = "coded"
CONSTANT EcfOnFraming = FALSE
INVARIANT AddressVerdictPhaseIndependent
