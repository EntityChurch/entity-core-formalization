\* §4.2 FINDING — NOT a negative control, and NOT a weakening. `RawWrites = FALSE` REMOVES §8's
\* direct tree:put from the model, leaving only the two paths §4.2.1 itself defines.
\*
\* The point is isolation. The obvious reading of the row above is "§8 permits a bypass, so of
\* course the tree can hold junk" -- which invites the answer "then forbid the bypass". This row
\* takes the bypass away and the defect survives, because §4.2.1's OWN non-trigger 1 puts a
\* failed-validation attestation into the tree and says so explicitly, and §4.2's walk reads the
\* tree. The closure is broken by the subsection that asserts it, without any help from §8.
\*
\* Expected verdict: `Invariant CacheMatchesValidated is violated.`
SPECIFICATION Spec

CONSTANTS
  NA                      = 3
  NQ                      = 2
  RawWrites               = FALSE
  AcceptInvalidates       = TRUE
  RawInvalidates          = FALSE
  ScopedInvalidation      = TRUE
  WalkClosedOverValidated = FALSE

INVARIANTS
  CacheMatchesValidated
