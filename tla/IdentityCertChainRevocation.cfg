\* §3.6 vs §ATTEST:TV-A8 FINDING -- NOT a negative control, and the only CROSS-SPEC row on this
\* track. RevocationAdmitted = FALSE and RevocationDispatched = FALSE are §3.6 AS WRITTEN: step
\* 1's gate excludes "revocation" (in an explicit comment) and `identity_topology_for`'s match
\* has no arm for it.
\*
\* §ATTEST:TV-A8 sets up an attestation whose signature is invalid because a raw `tree:put`
\* bypassed `:create` validation, gives the expected output as "A", and justifies it: "substrate
\* is signature-agnostic; consumers layer signature validation per topology -- identity's
\* `identity_verify_cert` rejects A at topology-dispatch step". For a revocation, identity's
\* topology-dispatch step is never reached.
\*
\* §ATTEST:3.3 says the opposite of §ATTEST:4.3 on the same question ("The attestation primitive
\* validates the signature on the revocation" vs "the substrate is signature-agnostic"), so the
\* delegation in TV-A8 is the document's own resolution of that -- and it points at a step that
\* does not exist. §3.6 step 3 then honours the revocation on `is_attestation_live` plus a field
\* comparison, neither of which is a signature check.
\*
\* THE STATE TLC EXHIBITS: `kind = 5` (revocation).
\*
\* Expected verdict: `Invariant RevocationReachesTopology is violated.`
SPECIFICATION Spec

CONSTANTS
  SignerSetIsSound        = TRUE
  TopologyFirst           = TRUE
  AgentPublicAllowed      = FALSE
  RevocationAdmitted      = FALSE
  RevocationDispatched    = FALSE
  HandoffSignerFromTarget = TRUE
  TargetGuarded           = TRUE
  HandoffIsDualSig        = TRUE
  HandoffIdentityEnforced = TRUE

INVARIANTS
  RevocationReachesTopology
