\* NEGATIVE CONTROL #3 — §5.9 Ruling 1 dropped (DoubleCount = TRUE): a single dispatch is
\* decremented at ingress AND again on forward. Expect TtlNotDoubleCounted VIOLATED (spent
\* exceeds the dispatch count), and DepthBrakeFirst to fall with it — over-charging TTL is
\* precisely how the backstop starts masking the deterministic brake.
\* Run: make tlc SPEC=Bounds CFG=BoundsDoubleCountBug
SPECIFICATION Spec
CONSTANT TtlSeed = 16
CONSTANT DepthCeiling = 4
CONSTANT MaxFanout = 2
CONSTANT MaxCapDepth = 3
CONSTANT DoubleCount = TRUE
CONSTANT ShareReasonCode = FALSE
INVARIANT TtlNotDoubleCounted
