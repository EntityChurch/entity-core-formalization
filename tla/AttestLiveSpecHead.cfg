\* §5.3 FINDING — NOT a negative control. Every constant here is the constant the green sweep
\* runs; nothing in the model is broken or weakened. The invariant is §5.3's OWN stated
\* contract ("returns the most recent live attestation in the chain") asserted against §5.3's
\* own transcribed algorithm, and it fails.
\*
\* The counterexample TLC prints is the three-link chain: the deepest link is live, so the
\* antecedent holds, and the walk returns null because the intermediate link is not live by
\* the full §4.3 predicate and §5.3 filters its successors by exactly that predicate.
\*
\* Expected verdict: `Invariant SpecHeadFindsLiveHead is violated.`
SPECIFICATION Spec

CONSTANTS
  N              = 3
  AllowCycles    = TRUE
  LiveTransitive = TRUE
  DagFiltersLive = TRUE

INVARIANTS
  SpecHeadFindsLiveHead
