\* NEGATIVE CONTROL. Row 1 reduced to the handler's grant alone — the single-`authority` call
\* made with the WRONG one of the two. MUST FAIL on CallerFacingNeedsCaller.
\* Its sibling AuthoritySelectRow1Signature makes the same call with the OTHER one and is a
\* FINDING rather than a control, because that one is the shape §6.3's own pseudocode has.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "grant-only"
CONSTANT PeerRootChecked = FALSE
INVARIANT CallerFacingNeedsCaller
