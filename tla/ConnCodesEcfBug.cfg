\* NEGATIVE CONTROL #7 — §4.11: "`400 non_canonical_ecf` is NOT conformant on the framing
\* arm [MUST]". ENTITY-CBOR-ENCODING §5.4 defines that code for CBOR TAG-POLICY violations
\* specifically — any major-type-6 item in a data-field position — and "your bytes are
\* truncated" is not "re-encode without the tag". §4.11 generalizes §5.2a's ruling for the
\* adjacent arm: THE CODE SELECTS THE CALLER'S REMEDY, so a code merely in the right family
\* is still wrong.
\* This control's target invariant is deliberately the ONLY one that covers the framing
\* code — see ConnCodes.tla's note above FramingIsNotEcf for why the cause table excludes
\* that row.
\* EXPECTED VERDICT: FramingIsNotEcf VIOLATED.
\* Run: make tlc SPEC=ConnCodes CFG=ConnCodesEcfBug
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT SeqReadingBug = FALSE
CONSTANT AuthBeforeAddress = FALSE
CONSTANT RefusalMode = "coded"
CONSTANT EcfOnFraming = TRUE
INVARIANT FramingIsNotEcf
