\* §6.3 NON-VACUITY WITNESS. Asserted in order to be VIOLATED; the violation exhibits the state
\* and IS the pass. A clean run here means the state is unreachable and the greens above are
\* statements about a model that never gets interesting.
\*
\* THIS ONE FOUND A REAL VACUITY BEFORE IT WAS A WITNESS. Under the green cfg's union of the
\* three cohort repairs, kinds 1-5 are all admitted and 6-7 are both pre-routed, so NOTHING was
\* rejected, the phase-2a unbind path was dead, and `LocalCreateNeverUnbinds` and
\* `UnbindOnlyOnRejection` were true of a model that never unbinds anything. `KForeign` exists
\* because this witness came back clean.
\*
\* Expected verdict: `Invariant WitnessUnbindReached is violated.`
SPECIFICATION Spec

CONSTANTS
  PreRouteQPublish       = TRUE
  PreRouteQUpdate        = TRUE
  SeedOnPreRoute         = TRUE
  RevocationAdmitted     = TRUE
  Phase2aScoped          = TRUE
  EmitOnFailure          = TRUE
  EmitOnSuccess          = FALSE
  UnbindOnHandlerFailure = FALSE

INVARIANTS
  WitnessUnbindReached
