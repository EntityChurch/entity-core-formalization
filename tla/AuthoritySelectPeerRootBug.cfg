\* NEGATIVE CONTROL. §6.8 row 3: a peer-root dispatch — the peer acting as tree owner — gets NO
\* handler-level check, because "checking it would make the handler-level check stricter than
\* the dispatch-level one for the same dispatch". This applies one anyway. MUST FAIL on
\* PeerRootNeverDenied. Without this row, row 3 would be in the model with no property over it.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = TRUE
INVARIANT PeerRootNeverDenied
