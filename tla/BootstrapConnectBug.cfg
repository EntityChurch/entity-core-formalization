\* NEGATIVE CONTROL #3 — connections served before `system/protocol/connect` is installed.
\* §6.9 bootstraps it precisely so it is "pre-authorized" and cannot be missing when the
\* first frame arrives.
\* EXPECTED VERDICT: ConnectPreAuthorized VIOLATED.
\* Run: make tlc SPEC=Bootstrap CFG=BootstrapConnectBug
SPECIFICATION Spec
CONSTANT AtomicPublish = TRUE
CONSTANT GateAll = TRUE
CONSTANT GateConnect = FALSE
INVARIANT ConnectPreAuthorized
