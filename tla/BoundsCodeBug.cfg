\* NEGATIVE CONTROL #4 — §4.10(b) Ruling 3 dropped (ShareReasonCode = TRUE): the continuation
\* causal-depth brake reports `chain_depth_exceeded`, the same reason string as the §4.10(b)
\* capability-chain depth limit. Expect ReasonCodesDistinct VIOLATED. §4.10(b): "Two
\* mechanisms, two reason codes — they MUST NOT share a reason string", because a caller that
\* cannot tell them apart cannot tell "your chain is too deep" (400, client-correctable
\* structural excess) from "your continuation ran too far" (429, suspend and resume).
\* Run: make tlc SPEC=Bounds CFG=BoundsCodeBug
SPECIFICATION Spec
CONSTANT TtlSeed = 16
CONSTANT DepthCeiling = 4
CONSTANT MaxFanout = 2
CONSTANT MaxCapDepth = 3
CONSTANT DoubleCount = FALSE
CONSTANT ShareReasonCode = TRUE
INVARIANT ReasonCodesDistinct
