\* §9.4 FINDING -- NOT a negative control, and THE HEADLINE OF THIS TRACK. Constants are §6.3 as
\* written; nothing is weakened.
\*
\* A compromise-recovery signed K-of-N by the identity's REAL quorum, delivered to a contact
\* that has ALREADY received that identity's genuine quorum-publish, is REJECTED. §9.4's
\* fail-closed branch fires on an anchor §6.3 never stored.
\*
\* §9.6 names compromise-recovery as the only remedy for a stolen controller or identifier key
\* ("Recovery: quorum signs an identity-rotation-recovery per §4.4 and §9.4"); §7.1 is a custody
\* model built to make it possible; §11.3 calls the configuration that provides it the
\* RECOMMENDED default. On the pinned text it does not complete cross-peer.
\*
\* AND THIS IS WHY THE PROHIBITION BESIDE IT IS NOT EVIDENCE. `RecoveryFailClosed` is green under
\* these exact constants -- because nothing is ever accepted. docs/status/SCOPING-2026-09-06-
\* IDENTITY-ATTESTATION.md §3.3 predicted the shape before a line was modeled ("a peer that does
\* nothing satisfies it ... write the witness before the prohibition"). The witness was written
\* first. It is this row.
\*
\* `rejGenuine` is what the invariant is stated over rather than the naive conjunction, because
\* a recovery delivered BEFORE the publish arrives is rightly rejected and an invariant that
\* could not tell those apart would report a defect on an ordinary out-of-order delivery.
\*
\* Expected verdict: `Invariant RecoveryAttainable is violated.`
SPECIFICATION Spec

CONSTANTS
  SeedEnabled           = FALSE
  SeedValidates         = TRUE
  FailClosed            = TRUE
  AnchorIsCachedPublish = TRUE
  AnchorDroppedOnAccept = FALSE

INVARIANTS
  RecoveryAttainable
