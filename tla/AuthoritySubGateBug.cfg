\* NEGATIVE CONTROL #3 — §5.2 "the condition is the field, not the door" dropped
\* (CheckSubDispatch = FALSE): the resource check is gated on a wire-entry predicate /
\* is_sub_dispatch flag instead of on `execute.data.resource is not null`. Expect
\* ResourceAuthorized VIOLATED — an in-process sub-dispatch carrying a resource target its
\* own authority does not cover is allowed. §5.2: "A sub-dispatch path that skips it leaves
\* `register` authorized by nothing."
\* Run: make tlc SPEC=Authority CFG=AuthoritySubGateBug
SPECIFICATION Spec
CONSTANT AuthorityModel = "three"
CONSTANT CheckSubDispatch = FALSE
CONSTANT InheritResource = FALSE
INVARIANT ResourceAuthorized
