\* NEGATIVE CONTROL #2 — §4.10/§4.9(b) admission control dropped (Admit = FALSE): requests
\* are admitted without checking the in-flight bound. Expect ResourceBounded VIOLATED on BOTH
\* conjuncts now: pending exceeds MaxPending = 2, and — because the reference lifetime is tied
\* to the admitted lifetime — all 3 store keys go live at once, exceeding MaxStore = 2. At
\* v0.8.0 only the pending conjunct could fail; the live-key conjunct was vacuous.
\* Run: make tlc SPEC=Store CFG=StoreAdmitBug
SPECIFICATION Spec
CONSTANT NReq = 4
CONSTANT MaxPending = 2
CONSTANT MaxStore = 2
CONSTANT Serialize = TRUE
CONSTANT Admit = FALSE
CONSTANT SilentDrop = FALSE
CONSTANT SyncRefs = TRUE
INVARIANT StoreRaceFree
INVARIANT NoUseAfterFree
INVARIANT ResourceBounded
INVARIANT CleanReject
