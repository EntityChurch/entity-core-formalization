\* NEGATIVE CONTROL #2 — §4.10/§4.9(b) admission control dropped (Admit = FALSE): requests
\* are admitted without checking the in-flight bound, so pending grows past MaxPending under
\* concurrent load. Expect ResourceBounded VIOLATED (pending reaches NReq = 3 > MaxPending = 2 —
\* unbounded resource growth, the §4.9(b) leak class). Run: make tlc SPEC=Store CFG=StoreAdmitBug
SPECIFICATION Spec
CONSTANT NReq = 3
CONSTANT MaxPending = 2
CONSTANT MaxStore = 2
CONSTANT Serialize = TRUE
CONSTANT Admit = FALSE
CONSTANT SilentDrop = FALSE
INVARIANT StoreRaceFree
INVARIANT ResourceBounded
INVARIANT CleanReject
