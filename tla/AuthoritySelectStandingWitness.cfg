\* NON-VACUITY WITNESS — MUST BE VIOLATED. Exhibits a STANDING LEG: a hop inside a chain a live
\* caller originated, which itself serves no live caller's request. That is the state §6.8's own
\* measurement is about (the inbox deliver token arriving at `system/tree:put` four hops on), and
\* if it were unreachable every row-2 result in this module would be vacuous.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT WitnessStandingLeg
