\* NEGATIVE CONTROL #4 — §5.2 "a sub-dispatch that names NO resource has NO resource" dropped
\* (InheritResource = TRUE): a child that named no resource inherits its parent's targets.
\* Expect NoManufacturedTarget VIOLATED. §5.2: "Inheritance manufactures a target the caller
\* never named, and any handler that reads the field acts on it: a handler sub-dispatching to
\* `register` without naming a resource would install at whatever path ITS caller happened to
\* name." Run: make tlc SPEC=Authority CFG=AuthorityInheritBug
SPECIFICATION Spec
CONSTANT AuthorityModel = "three"
CONSTANT CheckSubDispatch = TRUE
CONSTANT InheritResource = TRUE
INVARIANT NoManufacturedTarget
