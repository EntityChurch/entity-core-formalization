\* NON-VACUITY WITNESS (NEW at 0.8.2) — the TLC analog of ProVerif's reachability
\* queries, closing the gap disclosed in PROPERTIES.md §C.4: nothing in the TLA+ track
\* proved the modeled system ever reaches an interesting state, so a trivially-inert
\* model would have reported the same green as a working one.
\*
\* WitnessDispatched is asserted as an INVARIANT that MUST FAIL. A violation is the PASS
\* condition. EXPECTED VERDICT: VIOLATION.
\* Run: make tlc SPEC=Conn CFG=ConnWitness
SPECIFICATION Spec
CONSTANT MaxFrames = 3
CONSTANT Enforce = TRUE
CONSTANT DropFrame = FALSE
INVARIANT WitnessDispatched
