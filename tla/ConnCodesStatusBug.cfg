\* NEGATIVE CONTROL #2 — the other half of §4.7's sentence: "or returns a different status,
\* is non-conformant". The responder emits the right codes but a uniform 400, collapsing the
\* 401 authentication boundary (§4.6) and the 409 already-established case into the
\* client-correctable class.
\* EXPECTED VERDICT: StatusMatchesCode VIOLATED.
\* Run: make tlc SPEC=ConnCodes CFG=ConnCodesStatusBug
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = TRUE
CONSTANT PreHelloAuthRow = "r6_nonce"
INVARIANT StatusMatchesCode
