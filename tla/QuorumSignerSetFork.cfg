\* §4.2 FINDING — NOT a negative control. Every constant is the green sweep's constant.
\*
\* The unguarded form of the green `CohortMatchesNormativeOnChain`. §3.2 describes one
\* supersedes chain per quorum ("a quorum-update supersedes the previous quorum-update for the
\* same quorum"), and §6.2 -- which DOES validate `new_threshold >= 1` and
\* `new_threshold <= |new_signers|` -- validates nothing about the chain shape. `supersedes` is
\* an optional caller-supplied hash, so a forked or multi-rooted update graph is accepted.
\*
\* Off the single-chain shape there is no agreed answer: EXTENSION-ATTESTATION §5.3 breaks ties
\* toward the HIGHEST content_hash inside a walk, while entity-core-go and entity-core-py take
\* the LOWEST across probes and entity-core-rust takes whichever root its index yields first.
\* Three implementations, three tie-breaks, on input no stated rule rejects.
\*
\* Expected verdict: `Invariant CohortMatchesNormative is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = TRUE
  DescTemporal    = TRUE

INVARIANTS
  CohortMatchesNormative
