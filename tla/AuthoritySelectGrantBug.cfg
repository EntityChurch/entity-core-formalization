\* NEGATIVE CONTROL. `authority := ctx.handler_grant`, always — the flattened reading 0.8.2.22
\* names by name: "Flattening the intersection made a handler grant — broad by construction —
\* the authority for listing entries and merge expansions, which makes the listing filter
\* vacuous and lets a narrow caller merge anywhere the tree handler can reach." MUST FAIL on
\* CallerFacingNeedsCaller, §6.8 row 1's converse.
SPECIFICATION Spec
CONSTANT AuthoritySource = "grant"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT CallerFacingNeedsCaller
