\* NEGATIVE CONTROL #1 — §5.9 EQUAL MAGNITUDES (TtlSeed = DepthCeiling), the "9-vs-64"
\* cross-peer bound divergence 0.8.1 pins out: "equal magnitudes (both were 64) violate it by
\* letting TTL mask the deterministic brake." Expect DepthBrakeFirst VIOLATED — the chain
\* terminates on the TTL backstop and the deterministic depth brake never gets to fire, so two
\* peers with different fan-out disagree about where the chain stopped.
\* Run: make tlc SPEC=Bounds CFG=BoundsEqualBug
SPECIFICATION Spec
CONSTANT TtlSeed = 4
CONSTANT DepthCeiling = 4
CONSTANT MaxFanout = 2
CONSTANT MaxCapDepth = 3
CONSTANT DoubleCount = FALSE
CONSTANT ShareReasonCode = FALSE
INVARIANT DepthBrakeFirst
