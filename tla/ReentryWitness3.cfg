\* NON-VACUITY WITNESS (NEW at 0.8.2) — the TLC analog of ProVerif's reachability queries,
\* closing the gap disclosed in PROPERTIES.md §C.4.
\*
\* WitnessBothComplete is asserted as an INVARIANT that MUST FAIL. A violation is the PASS
\* condition: it exhibits a reachable state in which both peers complete the full reentrant
\* exchange, proving the liveness and frame-integrity results are not vacuously true of a
\* model in which the exchange never happens.
\* EXPECTED VERDICT: VIOLATION. Run: make tlc SPEC=Reentry CFG=ReentryWitness
SPECIFICATION Spec
CONSTANT N = 3
CONSTANT Serialized = FALSE
CONSTANT AtomicFrame = TRUE
INVARIANT WitnessBothComplete
