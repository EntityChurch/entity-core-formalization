\* §6.1 FINDING — NOT a negative control. `CreateValidates = FALSE` is §6.1 and §3.1 AS
\* WRITTEN: `:create` states no structural validation at all, §3.1 types `threshold` as
\* `primitive/uint`, and 0 is a uint. §9.1's MUST list adds nothing. Only §6.2 `:update`
\* constrains it -- "new_threshold >= 1; new_threshold <= |new_signers|" -- so the rule exists for
\* the operation that CHANGES a roster and not for the one that CREATES it.
\*
\* §4.1's last line is `return len(signed) >= threshold`. At threshold 0 that is `0 >= 0`: true,
\* with no signature found, no peer resolved and nothing verified. §2 calls this validator "the
\* only mechanism that distinguishes quorum from a regular peer node".
\*
\* All three implementations reject threshold 0 at their create path, supplying a rule the spec
\* writes elsewhere; entity-core-py's validator still opens `if threshold <= 0: return True`,
\* which is §4.1 reproduced exactly.
\*
\* Expected verdict: `Invariant KofNRequiresASignature is violated.`
SPECIFICATION Spec

CONSTANTS
  NS              = 3
  NP              = 3
  CreateValidates = FALSE
  DedupeByPeer    = TRUE
  ResolveChecked  = TRUE

INVARIANTS
  KofNRequiresASignature
