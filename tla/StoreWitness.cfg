\* NON-VACUITY WITNESS (NEW at 0.8.2) — the TLC analog of ProVerif's reachability queries,
\* closing the gap disclosed in PROPERTIES.md §C.4: nothing in the TLA+ track proved the
\* modeled system ever reaches an interesting state, so a trivially-inert model would have
\* reported the same green as a working one.
\*
\* WitnessStoreLive == store = {} is asserted as an INVARIANT that MUST FAIL. A violation is
\* the PASS condition: it exhibits a reachable state in which the content store is actually
\* live, proving the safety results above are not vacuously true of a model that never wrote.
\* EXPECTED VERDICT: VIOLATION (an "Invariant WitnessStoreLive is violated" counterexample).
\* Run: make tlc SPEC=Store CFG=StoreWitness
SPECIFICATION Spec
CONSTANT NReq = 4
CONSTANT MaxPending = 2
CONSTANT MaxStore = 2
CONSTANT Serialize = TRUE
CONSTANT Admit = TRUE
CONSTANT SilentDrop = FALSE
CONSTANT SyncRefs = TRUE
INVARIANT WitnessStoreLive
