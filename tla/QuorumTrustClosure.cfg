\* §4.2 FINDING — NOT a negative control. `WalkClosedOverValidated = FALSE` is §4.2 AS WRITTEN:
\* the walk is `find_attestations_targeting`, the EXTENSION-ATTESTATION §5.4 index lookup, and
\* nothing in §4.2 filters it to validated entries. Every other constant is the green's.
\*
\* §4.2.1 says in its own words that "the cache reflects validated quorum state, not raw tree
\* state". This asserts that sentence. It fails, because two other sentences in the same
\* document put unvalidated entities into the tree and no sentence anywhere takes them back out
\* on the read side:
\*   - §4.2.1 non-trigger 1: an attestation that FAILS K-of-N "may sit in the tree at a
\*     structurally-valid path without being authoritative";
\*   - §8: "Direct tree:put to system/quorum/... paths is permitted but bypasses the quorum
\*     handler's validation";
\*   - EXTENSION-ATTESTATION §5.7 I4: the indexes do not filter, and find_attestations_* keeps
\*     returning the entity.
\* Meanwhile §4.2's cold-start posture justifies trusting the walk on the ground that every
\* entry was "tree-bound only on validation success". That is the sentence this row refutes.
\*
\* Expected verdict: `Invariant CacheMatchesValidated is violated.`
SPECIFICATION Spec

CONSTANTS
  NA                      = 3
  NQ                      = 2
  RawWrites               = TRUE
  AcceptInvalidates       = TRUE
  RawInvalidates          = FALSE
  ScopedInvalidation      = TRUE
  WalkClosedOverValidated = FALSE

INVARIANTS
  CacheMatchesValidated
