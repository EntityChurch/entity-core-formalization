\* NEGATIVE CONTROL #2 — §6.10 v7.74 B2 carve-out dropped (MarkerDeletes = TRUE): a bind to a
\* system/deletion-marker fires event_type "deleted" instead of "modified" (conflating the
\* listing-visibility role with emit semantics). Expect EventTypeCorrect VIOLATED.
\* Run: make tlc SPEC=Emit CFG=EmitMarkerBug
SPECIFICATION Spec
CONSTANT Fire = TRUE
CONSTANT MarkerDeletes = TRUE
CONSTANT MaxOps = 3
CONSTANT StallEmit = FALSE
INVARIANT EventIffRealWork
INVARIANT NoEventOnNoop
INVARIANT EventTypeCorrect
