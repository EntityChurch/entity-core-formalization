\* NON-VACUITY WITNESS — asserted as an invariant that MUST FAIL. A violation exhibits a
\* reachable state in which every §4.7 row this model covers has actually been emitted,
\* proving the properties above are not vacuously true of a responder that never rejects.
\* This carries more weight here than in other modules: the code assignment is a function of
\* the constants, so REACHABILITY of each row is where this model's content lives.
\* EXPECTED VERDICT: VIOLATION. Run: make tlc SPEC=ConnCodes CFG=ConnCodesWitness
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT PreHelloAuthRow = "r6_nonce"
INVARIANT WitnessAllRowsEmitted
