\* NON-VACUITY WITNESS — asserted as an invariant that MUST FAIL. A violation exhibits a
\* reachable state in which every §4.7 row this model covers has been emitted AND every
\* §4.11 cause has fired, proving the properties are not vacuously true of a responder that
\* never rejects anything. This carries more weight here than in other modules: the code
\* assignment is a function of the constants, so REACHABILITY of each row is where this
\* model's content lives.
\* EXPECTED VERDICT: VIOLATION. Run: make tlc SPEC=ConnCodes CFG=ConnCodesWitness
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT SeqReadingBug = FALSE
CONSTANT AuthBeforeAddress = FALSE
CONSTANT RefusalMode = "coded"
CONSTANT EcfOnFraming = FALSE
INVARIANT WitnessAllRowsEmitted
