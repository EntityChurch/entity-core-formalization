\* §6.3 / §3.6 FINDING -- NOT a negative control. Constants are the spec as written.
\*
\* A `revocation` arriving over cross-peer sync is UNBOUND by phase 2a, because §3.6 step 1
\* rejects it: "revocation" is not "identity-cert" and §3.6 states in an explicit comment that
\* it is deliberately not in `identity_lifecycle_kinds()`.
\*
\* Every other part of the document expects it to arrive. §4.6 gives identity authority-
\* revocation rules over the kind; §5.1 stores revocations under `internal/cert/` AND
\* `public/cert/`; §10.2's sync hook fires `process_attestation` on `public/cert/` arrivals;
\* §12.4 has cross-peer trust updating by TOFU + supersedes. And §3.6's own step 3 reads live
\* revocations back out of the tree -- the tree phase 2a just deleted them from.
\*
\* COHORT: only entity-core-go survives this, and it needed TWO spec-absent additions to do so
\* -- `types.KindRevocation` in `isIdentityKind`, and a `KindRevocation` arm in
\* `IdentityTopologyFor` that §3.6's match does not have. Rust and Python both reject.
\*
\* THE STATE TLC EXHIBITS: `kind = 5`, `src = 2` (cross-peer sync).
\*
\* Expected verdict: `Invariant RevocationSurvivesArrival is violated.`
SPECIFICATION Spec

CONSTANTS
  PreRouteQPublish       = FALSE
  PreRouteQUpdate        = FALSE
  SeedOnPreRoute         = FALSE
  RevocationAdmitted     = FALSE
  Phase2aScoped          = TRUE
  EmitOnFailure          = TRUE
  EmitOnSuccess          = FALSE
  UnbindOnHandlerFailure = FALSE

INVARIANTS
  RevocationSurvivesArrival
