\* §4.2 NEGATIVE CONTROL. `ProbeAllUpdates = FALSE` weakens the model to §4.2 AS WRITTEN:
\* probe `updates[0]` only, instead of probing every candidate as all three implementations do.
\*
\* The green `CohortMatchesNormativeOnChain` must lose its teeth under it -- and the size of
\* that loss is the measure of how much work the cohort's undocumented workaround is doing. On
\* a well-formed chain of three, the single probe returns null and §4.2 falls through to the
\* creation-time roster where the normative sentence names the third link.
\*
\* Expected verdict: `Invariant CohortMatchesNormativeOnChain is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = FALSE
  DescTemporal    = TRUE

INVARIANTS
  CohortMatchesNormativeOnChain
