\* §9.4 NON-VACUITY WITNESS. Asserted in order to be VIOLATED; the violation exhibits the state
\* and IS the pass. On a module whose headline finding IS a vacuity, these are load-bearing
\* rather than a formality.
\*
\* The §9.4 trust anchor is ever non-empty. If it is not, every green on this module is a
\* statement about a contact with no anchor -- which is what §6.3 as written leaves it as.
\*
\* Expected verdict: `Invariant WitnessAnchorSeeded is violated.`
SPECIFICATION Spec

CONSTANTS
  SeedEnabled           = TRUE
  SeedValidates         = TRUE
  FailClosed            = TRUE
  AnchorIsCachedPublish = TRUE
  AnchorDroppedOnAccept = FALSE

INVARIANTS
  WitnessAnchorSeeded
