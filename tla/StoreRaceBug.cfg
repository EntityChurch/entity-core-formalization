\* NEGATIVE CONTROL #1 — §4.8 store-safety dropped (Serialize = FALSE): the write critical
\* section is entered without the single-writer guard, so two concurrent dispatch activities
\* mutate the store at once. Expect StoreRaceFree VIOLATED (writers reaches 2 — a data race).
\* Run: make tlc SPEC=Store CFG=StoreRaceBug
SPECIFICATION Spec
CONSTANT NReq = 4
CONSTANT MaxPending = 2
CONSTANT MaxStore = 2
CONSTANT Serialize = FALSE
CONSTANT Admit = TRUE
CONSTANT SilentDrop = FALSE
CONSTANT SyncRefs = TRUE
INVARIANT StoreRaceFree
INVARIANT NoUseAfterFree
INVARIANT ResourceBounded
INVARIANT CleanReject
