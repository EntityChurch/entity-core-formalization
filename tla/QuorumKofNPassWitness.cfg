\* §4.1 NON-VACUITY WITNESS. Asserted in order to be VIOLATED; the violation exhibits the
\* state and IS the pass. A clean run means the state is unreachable and the greens above are
\* statements about a model that never gets there.
\*
\* Expected verdict: `Invariant WitnessKofNPasses is violated.`
SPECIFICATION Spec

CONSTANTS
  NS              = 3
  NP              = 3
  CreateValidates = TRUE
  DedupeByPeer    = TRUE
  ResolveChecked  = TRUE

INVARIANTS
  WitnessKofNPasses
