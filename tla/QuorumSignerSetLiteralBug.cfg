\* §4.2 NEGATIVE CONTROL. `DescTemporal = FALSE` weakens EXTENSION-ATTESTATION §4.3's undefined
\* `not_expired` to its LITERAL reading -- the expires_at check alone.
\*
\* This is the control that proves the reading is load-bearing rather than editorial: with it,
\* even the cohort's probe-everything repair cannot stop a scheduled quorum-update from taking
\* the quorum to its genesis roster, because the pending successor kills its predecessor from
\* every probe point at once. A defect in a helper nobody defined, surviving a workaround written
\* for a different defect.
\*
\* Expected verdict: `Invariant CohortNeverSilentlyReverts is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = TRUE
  DescTemporal    = FALSE

INVARIANTS
  CohortNeverSilentlyReverts
