\* T4 NEGATIVE CONTROL — the mapped obligation must be able to FAIL for its stated reason.
\*
\* GateEstablished = FALSE is Core's existing §4.2 defect: a client dispatches before its
\* connection is established. Under the mapping that makes Conn's `dispatched` TRUE while
\* Conn's `everEstablished` is FALSE, so Conn's OWN DispatchedImpliesEstablished is violated.
\*
\* THIS IS A DIFFERENT CLAIM FROM CoreMapFreeCarried.cfg AND BOTH ARE NEEDED. That row shows
\* the MAPPING does not force the invariant (some type-correct valuation breaks it). This row
\* shows a real CORE BEHAVIOURAL DEFECT propagates through the mapping to the component's own
\* property — i.e. the implication is load-bearing on Core's transition relation, not only on
\* its type space. A mapping could pass the first test and still be blind to every actual
\* defect; that is the trap the T4 definition-of-done names.
\*
\* EXPECTED VERDICT: VIOLATION of RefinesConnDispatched, in a state where some peer has
\* cstate \in {"sent","done"} while its conn is still "new".
\* Run: make tlc SPEC=Core CFG=CoreRefinesGateBug
SPECIFICATION Spec
CONSTANT N = 2
CONSTANT Serialized = FALSE
CONSTANT AtomicFrame = TRUE
CONSTANT GateEstablished = FALSE
CONSTANT GateRevocation = TRUE
INVARIANT RefinesConnDispatched
