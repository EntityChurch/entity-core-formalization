\* §4.2 FINDING — NOT a negative control. Every constant is the green sweep's constant, and the
\* invariant names both readings explicitly rather than taking one from a constant.
\*
\* EXTENSION-ATTESTATION §4.3's descendant filter calls `not_expired`, which that document uses
\* and never defines -- the same hole this repo routed as F4, argued there on the neighbouring
\* `is_self_revoked`. Read literally it is the expires_at check alone, which is what the helper
\* is named after and what §4.3 writes four lines above as a check SEPARATE from `not_before`.
\* Under that reading a scheduled quorum-update -- one whose not_before has not arrived -- is
\* "not expired", so it counts as a live descendant and kills its predecessor without being
\* usable itself. Nothing is live, and the quorum drops to its genesis roster until the change
\* takes effect.
\*
\* All three implementations read it the other way (Go `shallowEffective`, Rust
\* `is_self_valid_basic`, Python `_is_temporally_invalid` all check not_before too), so this is
\* a defect in the text and the divergence is in the safe direction -- as F1 was.
\*
\* THE STATE TLC EXHIBITS: `sup = <<0, 0, 1>>`, `nb = <<0, 0, 2>>`, `xp = <<0, 0, 0>>`, at
\* t = 1. Update 3 supersedes update 1 and takes effect at instant 2. At instant 1 the cohort
\* reading answers "update 1" -- correct, the scheduled change has not landed -- and the literal
\* reading answers "the genesis roster". Two nodes are enough; the chain of three is not needed.
\*
\* Expected verdict: `Invariant NotExpiredReadingsAgree is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = TRUE
  DescTemporal    = TRUE

INVARIANTS
  NotExpiredReadingsAgree
