\* §4.1 NEGATIVE CONTROL. `ResolveChecked = FALSE` drops `if candidate_peer is null:
\* continue`, so a signature attributed to a peer this node cannot resolve is counted anyway.
\*
\* Expected verdict: `Invariant UnresolvableNeverCounts is violated.`
SPECIFICATION Spec

CONSTANTS
  NS              = 3
  NP              = 3
  CreateValidates = TRUE
  DedupeByPeer    = TRUE
  ResolveChecked  = FALSE

INVARIANTS
  UnresolvableNeverCounts
