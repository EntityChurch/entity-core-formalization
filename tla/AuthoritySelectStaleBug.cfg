\* NEGATIVE CONTROL. Same weakening again, third property, and this one gives
\* NoStaleTokenAuthority its teeth. MUST FAIL: §6.3 says `authority` is NOT "the capability on
\* the request", and §6.8 says the propagated field exists for exactly two purposes, neither of
\* which is being this check's authority. The violation exhibits the check running against a
\* token four hops old with unrelated scopes — stated structurally, before any consequence.
SPECIFICATION Spec
CONSTANT AuthoritySource = "propagated"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT NoStaleTokenAuthority
