\* NON-VACUITY WITNESS — MUST BE VIOLATED. The 0.8.2.21 -> 0.8.2.22 discriminator correction was
\* SEMANTIC and not editorial: there is a reachable access on which "who named the path" and
\* "whether it serves a live caller's request" return DIFFERENT VERDICTS.
\*
\* Asserted because this repo carried the superseded wording in `docs/STATUS.md` §Next item
\* 0****** for weeks, and a model transcribed from that worklist would have encoded the refuted
\* rule and gone green on it (AGENTS.md D12 — a model that encodes a paraphrase proves a theorem
\* about the paraphrase). This row is the measurement that the difference is real.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT WitnessReadingsDiverge
