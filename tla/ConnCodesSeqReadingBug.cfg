\* THE FINDING, RUN AS A CONTROL — and the only row in this repo's matrix whose "defect" is
\* a CONFORMANT READING OF THE SPEC rather than something we injected.
\*
\* §4.7 table row 10 reads "Out-of-order operation (e.g., authenticate before hello) |
\* `connection_sequence_error` | 400". §4.6 step 1 reads "an `authenticate` received before
\* any hello nonce was issued MUST be rejected with status 401 `invalid_nonce`". Both are
\* normative MUSTs naming the same input, and they disagree on the code AND the status class.
\* This config selects §4.7's reading; PreHelloAuthIsInvalidNonce is §4.6 step 1 transcribed.
\*
\* EXPECTED VERDICT: PreHelloAuthIsInvalidNonce VIOLATED — which is the two clauses
\* contradicting each other, exhibited. Routed to entity-core-protocol as a proposal; this
\* repo does not edit the spec (AGENTS.md).
\* Run: make tlc SPEC=ConnCodes CFG=ConnCodesSeqReadingBug
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT PreHelloAuthRow = "r10_seq"
INVARIANT PreHelloAuthIsInvalidNonce
