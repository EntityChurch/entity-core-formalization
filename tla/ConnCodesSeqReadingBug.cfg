\* NEGATIVE CONTROL #3 — AND READ THIS HEADER, BECAUSE THE ROW CHANGED MEANING.
\*
\* ⭐ AT THE v0.8.2 PIN THIS WAS A FINDING IN SUBSTANCE, NOT A CONTROL. Its "defect" was a
\* CONFORMANT READING OF THE SPEC: §4.7 row 6 said a pre-hello `authenticate` is 401
\* `invalid_nonce` (citing §4.6 step 1) and row 10's parenthetical — four rows below, same
\* table, also a MUST — said 400 `connection_sequence_error`. This repo routed it arguing
\* that row 10's PARENTHETICAL ALONE was the defect, four words wide.
\*
\* ⭐ 0.8.2.1 (FM-1) ADOPTED THAT. At .25 the row reads "**Not** a pre-hello `authenticate`
\* (see below)" and a following paragraph pins the input to 401 by §4.2, §4.6 step 1 and
\* row 6. So this row DEMOTES: a finding row retires when the spec moves to where we argued
\* it should, and what is left is an ordinary injected defect — a peer that routes a
\* pre-hello authenticate to the out-of-order row against text that names that exact input.
\*
\* ⛔ AND IT WAS IN THE WRONG GATE TABLE THE WHOLE TIME — found while retargeting, 2026-09-15.
\* This row has sat in `TLC_NEG` since it was written, whose own header reads "every row MUST
\* fail — a green here means the property has no teeth." That sentence was FALSE of this row
\* at the pin: a green would have meant the defect was FIXED UPSTREAM and the row should be
\* RETIRED. `TLC_FINDING` exists precisely for that second meaning; it was created on
\* 2026-09-07 for the attestation track and THIS ROW, which predates it and has the identical
\* shape, was never moved. D14's own failure — enumerate the class, do not fix the instance —
\* committed by the session that wrote the rule.
\*
\* ⭐ It is in the RIGHT table now without anything being moved, because the spec moved
\* instead: at .25 this really is an ordinary injected defect and `TLC_NEG`'s sentence is
\* true of it. **A row that was misfiled and is now correctly filed, by an upstream fix, is
\* the hardest kind of defect to notice** — nothing was ever red and nothing is red now.
\* EXPECTED VERDICT: PreHelloAuthIsInvalidNonce VIOLATED.
\* Run: make tlc SPEC=ConnCodes CFG=ConnCodesSeqReadingBug
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT SeqReadingBug = TRUE
CONSTANT AuthBeforeAddress = FALSE
CONSTANT RefusalMode = "coded"
CONSTANT EcfOnFraming = FALSE
INVARIANT PreHelloAuthIsInvalidNonce
