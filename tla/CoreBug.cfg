\* NEGATIVE CONTROL — §6.11 serialization defect (Serialized = TRUE): each peer holds its
\* per-connection write mutex across send+recv. Expect "Deadlock reached" — the Class-G
\* bidirectional reentry deadlock forms even under full composition (handshake + store +
\* revocation gate did not mask it), breaking EventuallyResolved. Run: make tlc SPEC=Core CFG=CoreBug
SPECIFICATION Spec
CONSTANT Serialized = TRUE
CONSTANT GateEstablished = TRUE
CONSTANT GateRevocation = TRUE
INVARIANT StoreBounded
INVARIANT DispatchNeedsEstablished
INVARIANT ServeNeedsEstablished
INVARIANT NoServeWhenRevoked
PROPERTY EventuallyResolved
