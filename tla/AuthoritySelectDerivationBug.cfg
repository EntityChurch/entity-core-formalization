\* NEGATIVE CONTROL — THE SUPERSEDED DISCRIMINATOR, AND IT IS THE ONE THIS REPO ITSELF HAD
\* WRITTEN DOWN. `docs/STATUS.md` §Next item 0****** stated the rule as "selected by WHO NAMED
\* THE PATH" from 0.8.2.21 until 2026-09-16; 0.8.2.22 corrected it and names that reading
\* explicitly as what the rule is NOT ("Derivation is not the discriminator. A path the handler
\* derived is still the caller's access when its existence, its content, or its effect reaches
\* the caller"). MUST FAIL on CallerFacingNeedsCaller, on exactly the derived paths 0.8.2.22
\* enumerates — a listing entry, a merge expansion, a snapshot binding, a subscription payload.
\* A model transcribed from our own worklist would have encoded this and gone green (D12).
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "derivation"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT CallerFacingNeedsCaller
