\* §3.6 NON-VACUITY WITNESS. Asserted in order to be VIOLATED; the violation exhibits the state
\* and IS the pass. A clean run means the state is unreachable and the greens above are
\* statements about a model that never gets interesting.
\*
\* The contested cell is REACHABLE UNDER §4.2's TABLE: an admissible agent cert at mode=public
\* in the three-key default, signed by a controller. Without it, finding I4 would be a violation
\* of something no configuration reaches. Note the constants are §4.2a's reading, not the green
\* sweep's -- the witness is for the finding, and says so.
\*
\* Expected verdict: `Invariant WitnessPublicAgentCert is violated.`
SPECIFICATION Spec

CONSTANTS
  SignerSetIsSound        = TRUE
  TopologyFirst           = TRUE
  AgentPublicAllowed      = TRUE
  RevocationAdmitted      = TRUE
  RevocationDispatched    = TRUE
  HandoffSignerFromTarget = TRUE
  TargetGuarded           = TRUE
  HandoffIsDualSig        = TRUE
  HandoffIdentityEnforced = TRUE

INVARIANTS
  WitnessPublicAgentCert
