\* §5.9 / §4.10(b)-correct bound configuration — expect all-green.
\* Ceiling 4, worst-case fan-out 2, TTL seed 16: worst-case spend to the ceiling is
\* 4 x 2 = 8 < 16, so the deterministic depth brake engages strictly before the TTL backstop.
\* Magnitudes are scaled down from the spec's informative defaults (64 / 512) — §4.10 makes
\* the numbers explicitly non-normative, so the RATIO is the modeled content.
SPECIFICATION Spec
CONSTANT TtlSeed = 16
CONSTANT DepthCeiling = 4
CONSTANT MaxFanout = 2
CONSTANT MaxCapDepth = 3
CONSTANT DoubleCount = FALSE
CONSTANT ShareReasonCode = FALSE
INVARIANT DepthBrakeFirst
INVARIANT TtlNotDoubleCounted
INVARIANT ReasonCodesDistinct
INVARIANT DepthBoundedBySpend
