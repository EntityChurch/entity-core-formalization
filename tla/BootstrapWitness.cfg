\* NON-VACUITY WITNESS — asserted as an invariant that MUST FAIL. A violation exhibits a
\* reachable state in which initialization completed, a handler actually registered and a
\* connection actually established, proving the properties above are not vacuously true of a
\* system that never finishes booting.
\* EXPECTED VERDICT: VIOLATION. Run: make tlc SPEC=Bootstrap CFG=BootstrapWitness
SPECIFICATION Spec
CONSTANT AtomicPublish = TRUE
CONSTANT GateAll = TRUE
CONSTANT GateConnect = TRUE
INVARIANT WitnessBootComplete
