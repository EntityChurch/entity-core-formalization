\* NEGATIVE CONTROL #1 — §6.10 event-iff-real-work dropped (Fire = FALSE): every Store/Bind
\* step fires an event unconditionally, including re-put of an existing hash and re-bind to the
\* current hash. Expect EventIffRealWork / NoEventOnNoop VIOLATED (an event on a no-op).
\* Run: make tlc SPEC=Emit CFG=EmitFireBug
SPECIFICATION Spec
CONSTANT Fire = FALSE
CONSTANT MarkerDeletes = FALSE
CONSTANT MaxOps = 3
CONSTANT StallEmit = FALSE
INVARIANT EventIffRealWork
INVARIANT NoEventOnNoop
INVARIANT EventTypeCorrect
