\* NEGATIVE CONTROL #2 — §5.10 Layer-1 leak (LeakLayer1 = TRUE): a purely-local banlist bit
\* (Layer 2) modulates the Layer-1 chain verdict. Expect VerdictFnOfLayer1 VIOLATED — two peers
\* with the same `t` and the same observed-revocation state reach DIFFERENT verdicts because
\* their local banlists differ (local policy masquerading as protocol). Run: make tlc SPEC=Revoke CFG=RevokeLeakBug
SPECIFICATION Spec
CONSTANT HonorRevocation = TRUE
CONSTANT LeakLayer1 = TRUE
CONSTANT NoConverge = FALSE
INVARIANT RevokedNeverPasses
INVARIANT VerdictFnOfLayer1
PROPERTY RevocationConverges
