\* NON-VACUITY WITNESS — MUST BE VIOLATED. Exhibits a DERIVED path (a listing entry, a merge
\* expansion) serving a live caller and being ALLOWED under §6.8 row 1. Without it,
\* CallerFacingNeedsCaller would be true of a model in which no handler ever derives a path —
\* and the derived half is what the whole 0.8.2.22 correction is about.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT WitnessDerivedAllowed
