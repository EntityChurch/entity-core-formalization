\* §3.6 / §4.3 FINDING -- NOT a negative control. HandoffIdentityEnforced = FALSE is the pinned
\* text: §4.3's parenthetical "(att.attesting is old key = target.attested)" is an ASSERTION, and
\* §6.0b, §6.0c and §3.6 between them validate every other field of a handoff and not this one.
\* Every other constant is the green sweep's.
\*
\* §3.6's dual-sig arm requires signatures from `[target.attested, att.attested]`. Go requires
\* them from `[att.Attesting, att.Attested]` -- it never looks the target up. The two are the
\* same rule exactly when the unenforced identity holds; when it does not they demand signatures
\* from DIFFERENT KEYS, and the key Go demands is one the attestation's own author supplied.
\*
\* This is the AttestRevoke `DescReadingsCoincide` shape -- two readings of one under-specified
\* line -- and this time they separate.
\*
\* THE STATE TLC EXHIBITS: `kind = 2` (identity-rotation-handoff), `tres = TRUE` (the target
\* resolves), `tmatch = FALSE`.
\*
\* Expected verdict: `Invariant HandoffReadingsCoincide is violated.`
SPECIFICATION Spec

CONSTANTS
  SignerSetIsSound        = TRUE
  TopologyFirst           = TRUE
  AgentPublicAllowed      = FALSE
  RevocationAdmitted      = TRUE
  RevocationDispatched    = TRUE
  HandoffSignerFromTarget = TRUE
  TargetGuarded           = TRUE
  HandoffIsDualSig        = TRUE
  HandoffIdentityEnforced = FALSE

INVARIANTS
  HandoffReadingsCoincide
