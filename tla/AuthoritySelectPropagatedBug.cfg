\* NEGATIVE CONTROL (weakens the model AWAY from the spec — cf. the finding cfgs, which do not).
\* `authority := ctx.caller_capability`, always: the reading §6.3 records TWO SEATS SHIPPING
\* while the parameter was still called `capability` (through 0.8.2.20). MUST FAIL on
\* NoDeputySubstitution — §6.8 row 2's confused-deputy rule, the one direction that exists at
\* the core pin. The counterexample is a standing leg authorized by the originating caller's
\* (broader) capability on a path the executing handler's own grant does not cover.
SPECIFICATION Spec
CONSTANT AuthoritySource = "propagated"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT NoDeputySubstitution
