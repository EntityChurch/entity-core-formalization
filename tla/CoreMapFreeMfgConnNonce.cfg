\* T4 CLASSIFIER — MANUFACTURED. §4.6 step 1 nonce-echo — Core has no nonce (ledger T1).
\*
\* READ THE VERDICT THE RIGHT WAY ROUND: here a clean GREEN is the finding, not a success.
\* It shows the invariant holds in EVERY type-correct valuation, so it is a tautology of the
\* refinement mapping and says nothing about Core. A violation here would mean the opposite
\* — that Core's state can decide it — and would move this row to CARRIED.
\*
\* EXPECTED VERDICT: no error (invariant holds over the full type space).
\* Run: make tlc SPEC=CoreMapFree CFG=CoreMapFreeMfgConnNonce
SPECIFICATION Spec
CONSTANT N = 2
CONSTANT RefPeer = 1
INVARIANT FreeConnNonce
