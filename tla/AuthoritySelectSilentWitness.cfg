\* ⭐ NON-VACUITY WITNESS — MUST BE VIOLATED — AND IT IS THE MEASUREMENT THIS MODULE WAS BUILT
\* FOR. §6.8, verbatim: "the defect is wire-invisible, because both readings produce a
\* well-formed response and differ only in which authority was consulted."
\*
\* The violation exhibits a hop on which the CORRECT selection and the propagated-field reading
\* BOTH ALLOW — identical verdict, identical response, nothing for an oracle to grade — while
\* consulting DIFFERENT authorities. A conformance vector keyed on the response cannot separate
\* them, which is `entity-system-conformance`'s `ECP-R24` shape and the third disposition their
\* `GUIDE-CONFORMANCE` §5.2a does not have: a machine-checked model.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "serves"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT WitnessSilentSubstitution
