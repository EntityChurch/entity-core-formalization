\* NEGATIVE CONTROL. "Who initiated the chain" — the other reading §6.8 names as NOT the test,
\* and the one an implementation keyed on DYNAMIC EXTENT computes (a thread-local
\* current-caller). MUST FAIL on OwnBehalfNotSpuriouslyDenied.
\*
\* ⭐ READ THE INVARIANT IT BREAKS, BECAUSE IT IS THE RESULT. Under §6.8 as written this
\* misreading costs AVAILABILITY and not AUTHORITY: row 1 demands strictly more than row 2, so
\* misfiling an own-behalf access into row 1 can only refuse it. The escalation requires row 1's
\* ceiling to be ABSENT as well — see AuthoritySelectDeputy.cfg, which is a FINDING row and not
\* this one.
SPECIFICATION Spec
CONSTANT AuthoritySource = "selected"
CONSTANT Discriminator = "initiator"
CONSTANT Row1Authorities = "both"
CONSTANT PeerRootChecked = FALSE
INVARIANT OwnBehalfNotSpuriouslyDenied
