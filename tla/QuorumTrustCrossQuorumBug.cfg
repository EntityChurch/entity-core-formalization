\* §4.2.1 NEGATIVE CONTROL for non-trigger 3 / TV-QF15. `ScopedInvalidation = FALSE` lets one
\* quorum's event clear every cache. §4.2.1: "Activity on `quorum_id_other` MUST NOT invalidate
\* the cache for `quorum_id`. Cache entries are independently scoped per `quorum_id`."
\*
\* Expected verdict: `Invariant InvalidationScopedPerQuorum is violated.`
SPECIFICATION Spec

CONSTANTS
  NA                      = 3
  NQ                      = 2
  RawWrites               = TRUE
  AcceptInvalidates       = TRUE
  RawInvalidates          = FALSE
  ScopedInvalidation      = FALSE
  WalkClosedOverValidated = TRUE

INVARIANTS
  InvalidationScopedPerQuorum
