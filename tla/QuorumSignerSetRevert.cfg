\* §4.2 FINDING — NOT a negative control. Every constant is the green sweep's constant.
\*
\* THE SECURITY-SHAPED ONE. When EXTENSION-ATTESTATION §5.3's forward walk returns null, §4.2
\* does not error and does not return an empty set: `signers` still holds `quorum.data.signers`,
\* the roster frozen into the quorum entity at `:create`. So the resolver hands back the
\* CREATION-TIME roster at an instant when a quorum-update is live and in force -- and a
\* quorum-update that removed a compromised signer is undone by the very function that exists
\* to apply it.
\*
\* THE STATE TLC EXHIBITS: `sup = <<0, 1, 2>>`, `nb = <<0, 0, 0>>`, `xp = <<0, 0, 0>>`. An
\* ordinary quorum whose membership has been changed three times. Nothing expired, nothing
\* scheduled, nothing revoked, no fork. Probing the oldest of the three updates -- which is what
\* `updates[0]` may be, since EXTENSION-ATTESTATION §5.4 defines no order -- yields null, and
\* §4.2 falls through.
\*
\* The invariant is guarded by `WellFormedChain` deliberately. Unguarded it is violated by a
\* smaller state (a probe of an already-dead update among unrelated roots) which is a true
\* instance but NOT the mechanism being routed; the guard leaves exactly the linear chain on
\* three nodes, so the counterexample is the claim.
\*
\* Expected verdict: `Invariant SpecNeverSilentlyReverts is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = TRUE
  DescTemporal    = TRUE

INVARIANTS
  SpecNeverSilentlyReverts
