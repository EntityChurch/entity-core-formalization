\* §5.2-correct dispatch authority (0.8.2) — expect all-green.
\* THE JOINT-SATISFIABILITY CHECK for the three-valued claim: NoGrantlessAllow (security)
\* and EntryNotSpuriouslyDenied (availability) hold TOGETHER here. The two option-* controls
\* each hold one and break the other, which is what makes the third state normative.
SPECIFICATION Spec
CONSTANT AuthorityModel = "three"
CONSTANT CheckSubDispatch = TRUE
CONSTANT InheritResource = FALSE
INVARIANT NoGrantlessAllow
INVARIANT EntryNotSpuriouslyDenied
INVARIANT ResourceAuthorized
INVARIANT NoManufacturedTarget
