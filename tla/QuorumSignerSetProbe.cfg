\* §4.2 FINDING — NOT a negative control. Every constant is the green sweep's constant.
\*
\* §4.2 walks forward from `updates[0]`. `updates` comes from EXTENSION-ATTESTATION §5.4
\* `find_attestations_targeting`, which returns a LIST out of a field index and specifies no
\* order. This asserts the answer does not depend on which element that is. It does: on a chain
\* of three, probing the OLDEST update yields null (the EXTENSION-ATTESTATION §5.3 walk cannot
\* traverse it -- the finding this repo routed as F1) and §4.2 then falls through to the
\* creation-time roster, while probing the middle element yields the true head.
\*
\* One tree, two answers, chosen by index iteration order. None of the three implementations
\* does what §4.2 writes here; all three probe more than one element, which is a cohort
\* consistently working around an ordering the spec never pinned.
\*
\* THE STATE TLC EXHIBITS: `sup = <<0, 1, 2>>` with no expiry and no not_before -- the plain
\* three-update chain. Guarded by `WellFormedChain` for the reason given in the module header.
\*
\* Expected verdict: `Invariant ResultIndependentOfProbe is violated.`
SPECIFICATION Spec

CONSTANTS
  N               = 3
  T               = 2
  ProbeAllUpdates = TRUE
  DescTemporal    = TRUE

INVARIANTS
  ResultIndependentOfProbe
