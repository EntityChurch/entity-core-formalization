\* NON-VACUITY WITNESS for SpecWalkNeverExhausts. Asserted in order to be VIOLATED.
\*
\* "The §5.3 walk never runs out of fuel" is satisfied completely by a walk that never takes a
\* step, and that reading would make §5.3's missing depth bound look safe for a reason that
\* has nothing to do with depth. The violation exhibits a start whose returned head is a
\* DIFFERENT node -- so the walk does advance, and the bound is a real bound on a real walk.
\* Expected verdict: `Invariant WitnessSpecWalkSteps is violated.`
SPECIFICATION Spec

CONSTANTS
  N              = 3
  AllowCycles    = TRUE
  LiveTransitive = TRUE
  DagFiltersLive = TRUE

INVARIANTS
  WitnessSpecWalkSteps
