\* §4.7 + §4.11 conformant — expect all-green.
\*
\* ⛔ THIS CFG IS ABOUT spec-data/v0.8.2.25, NOT THE TRACK PIN. See ConnCodes.tla's
\* MODELING-PIN-OVERRIDE block; the override is declared in TRACKS.toml and gated by
\* `make trackcheck` in both directions.
\*
\* The contested cell is GONE. At the v0.8.2 pin this file carried
\* `PreHelloAuthRow = "r6_nonce"` because §4.7 gave two normative answers for a pre-hello
\* `authenticate` and the model checked both. 0.8.2.1 (FM-1) narrowed row 10's
\* parenthetical exactly as this repo argued (docs/PROPERTIES.md §D.1), so at .25 there is
\* one answer and `SeqReadingBug` is an ordinary injected defect rather than a reading.
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT SeqReadingBug = FALSE
CONSTANT AuthBeforeAddress = FALSE
CONSTANT RefusalMode = "coded"
CONSTANT EcfOnFraming = FALSE
INVARIANT TypeOK
INVARIANT ReasonCodesDistinct
INVARIANT StatusMatchesCode
INVARIANT PreHelloAuthIsInvalidNonce
INVARIANT StateConflictsAre409
INVARIANT AddressVerdictPhaseIndependent
INVARIANT PreAdmissionRefusalIsCoded
INVARIANT NoSilentDrop
INVARIANT NoBareCloseOnly
INVARIANT RefusalCodeMatchesCause
INVARIANT FramingIsNotEcf
