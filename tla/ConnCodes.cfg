\* §4.7-correct connection error codes (0.8.2 second gate audit) — expect all-green.
\* PreHelloAuthRow = "r6_nonce" is §4.6 STEP 1's explicit reading: "an `authenticate`
\* received before any hello nonce was issued MUST be rejected with status 401
\* `invalid_nonce`". §4.7's own table row 10 says otherwise; ConnCodesSeqReadingBug.cfg
\* runs that reading. The disagreement is a spec finding, not a modeling choice — see
\* ConnCodes.tla's header and ../docs/PROPERTIES.md §D.
SPECIFICATION Spec
CONSTANT CollapseCodes = FALSE
CONSTANT WrongStatus = FALSE
CONSTANT PreHelloAuthRow = "r6_nonce"
INVARIANT TypeOK
INVARIANT ReasonCodesDistinct
INVARIANT StatusMatchesCode
INVARIANT PreHelloAuthIsInvalidNonce
