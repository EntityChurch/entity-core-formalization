\* NEGATIVE CONTROL #2 — registration admitted as soon as `system/handler` is dispatchable,
\* which is the obvious reading (that is the handler doing the work) and is not what §6.9
\* says: "Three handlers MUST exist ... before any handler registration can occur." The
\* registration then runs while `system/tree` — which it writes to — is still installing.
\* EXPECTED VERDICT: NoRegistrationBeforeBootstrap VIOLATED.
\* Run: make tlc SPEC=Bootstrap CFG=BootstrapGateBug
SPECIFICATION Spec
CONSTANT AtomicPublish = TRUE
CONSTANT GateAll = FALSE
CONSTANT GateConnect = TRUE
INVARIANT NoRegistrationBeforeBootstrap
