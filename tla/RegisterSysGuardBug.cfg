\* NEGATIVE CONTROL #2 — §6.2 system-path guard dropped (GuardSystem = FALSE): a user-installed
\* handler is allowed to register at a system/* path. Expect NoUserAtSystem VIOLATED — the
\* user handler "hSys" becomes present at a system path. Run: make tlc SPEC=Register CFG=RegisterSysGuardBug
SPECIFICATION Spec
CONSTANT Atomic = TRUE
CONSTANT GuardSystem = FALSE
CONSTANT WedgeReg = FALSE
INVARIANT NoPartialResidue
INVARIANT RegisterAllOrNothing
INVARIANT IndexMatchesTree
INVARIANT NoUserAtSystem
