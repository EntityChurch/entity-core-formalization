\* NON-VACUITY WITNESS for every liveness result in this module. Asserted to be VIOLATED.
\*
\* Revocation actually FIRES somewhere: an unexpired attestation with no live descendant is
\* nonetheless dead, which can only be the revocation clause. Without this, `Live` could be
\* "not expired and not superseded" over this whole state space and every green above would
\* hold while saying nothing about revocation at all.
\* Expected verdict: `Invariant WitnessRevocationKills is violated.`
SPECIFICATION Spec

CONSTANTS
  N                = 4
  Peers            = {1, 2}
  AuthorityRevoke  = FALSE
  RecursiveMainRev = TRUE

INVARIANTS
  WitnessRevocationKills
