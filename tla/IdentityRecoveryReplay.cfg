\* §5.1 / §9.4 FINDING -- NOT a negative control. AnchorDroppedOnAccept = TRUE is a reading
\* §5.1's retention floor PERMITS: "the previous-handle cache entry ... MUST be retained at
\* least through the in-flight window (until the verifier accepts the new handle)". At the
\* instant of accept the floor expires, and §6.3's `update_handle_cache_to` side effect is
\* unspecified enough that dropping the old key is the natural implementation -- entity-core-go
\* does exactly `Set(new); Remove(old)`. Every other constant is the green sweep's.
\*
\* A DUPLICATE DELIVERY of the same rotation-recovery then keys on `properties.old_handle`, finds
\* nothing, and fail-closes. One attestation, two deliveries, two verdicts -- against §6.3's own
\* stated intent ("identical events at the same instant collapse to the same path -- desired
\* idempotent semantic") and with nothing in §5.2's sync surface promising exactly-once delivery.
\*
\* THIS IS NOT A FINDING AGAINST GO. Removal AT accept satisfies "retained until accept"
\* literally. The floor is set one instant too early; that is the defect.
\*
\* Expected verdict: `Invariant RecoveryIdempotent is violated.`
SPECIFICATION Spec

CONSTANTS
  SeedEnabled           = TRUE
  SeedValidates         = TRUE
  FailClosed            = TRUE
  AnchorIsCachedPublish = TRUE
  AnchorDroppedOnAccept = TRUE

INVARIANTS
  RecoveryIdempotent
