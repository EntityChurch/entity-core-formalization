\* §3.6 NEGATIVE CONTROL -- weakens the model on purpose, and it is the ONE THAT SHOWS WHAT THIS
\* WHOLE TRACK RESTS ON. SignerSetIsSound = FALSE withdraws the declared assumption that
\* §QUORUM:4.2 `current_signer_set` returns the roster its own normative sentence asks for.
\*
\* Seven routed quorum findings say it may not: Q1 has it hand back the CREATION-TIME roster on a
\* plain chain of three quorum-updates, with nothing expired and nothing scheduled. §3.6 calls it
\* from the top-level-controller arm, the rotation-recovery arm and the retirement arm -- so
\* every K-of-N verdict this extension reaches is taken against whatever comes back.
\*
\* THIS IS A CONTROL AND NOT A FINDING, deliberately. The defect is already routed as Q1 and
\* re-routing it wearing an identity section number is the double-count the prior checkpoint
\* warned about when it said the transcribe-or-assume choice is invisible afterwards. Making it a
\* control is how the choice is made visible: the identity greens are true ON THIS ASSUMPTION,
\* and here is the run that shows the assumption is load-bearing. LEAN-SEAM O16.
\*
\* Expected verdict: `Invariant KofNAnswerIsTrustworthy is violated.`
SPECIFICATION Spec

CONSTANTS
  SignerSetIsSound        = FALSE
  TopologyFirst           = TRUE
  AgentPublicAllowed      = FALSE
  RevocationAdmitted      = TRUE
  RevocationDispatched    = TRUE
  HandoffSignerFromTarget = TRUE
  TargetGuarded           = TRUE
  HandoffIsDualSig        = TRUE
  HandoffIdentityEnforced = TRUE

INVARIANTS
  KofNAnswerIsTrustworthy
