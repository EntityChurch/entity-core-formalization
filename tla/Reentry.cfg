\* Add statements after this line.
\* FIXED variant (V7 §6.11(a)+(b)+(a′) all honored) — expect all-green.
\* THE JOINT-SATISFIABILITY CHECK: this single config asserts both the liveness property (a)
\* protects and the safety property (a′) protects. §6.11(a′) claims per-frame write
\* serialization "does not reintroduce the (a) prohibition"; green here is that claim.
SPECIFICATION Spec
CONSTANT Serialized = FALSE
CONSTANT AtomicFrame = TRUE
INVARIANT NoDispatchWithoutGate
INVARIANT FramesNotInterleaved
PROPERTY EventuallyResolved
